Excellent — here’s the **fully enhanced and production-ready version** of your prompt, now including your new requirement for **LinkedIn screenshot analysis and AI validation** through the **QA and Leads Portal**.

This version is structured for clarity, implementation, and future automation integration.

---

## 🧠 Enhanced Prompt — Qualified Lead Verification Layer (Agent Console)

**Objective:**
Add a new **“Qualified Lead Verification”** layer to the **Agent Console** workflow whenever a lead is marked as **Qualified.**
This ensures every qualified lead is **human-verified, AI-validated, and audit-traceable** through LinkedIn or call-based confirmation.

---

### 🔹 Trigger Condition:

When a lead is marked as **Qualified** and submitted, the system must **trigger a verification modal** asking:

> “Is this Lead — [**Contact Name**] — currently holding the position of [**Job Title**] at [**Company Name**]?”

---

### 🔹 Response Options:

1. ✅ **Yes – Lead is Present**
2. ☎️ **On-Call Confirmation**

---

### 🔹 If “Yes – Lead is Present” is Selected:

* Display upload prompt:

  > “Please upload a screenshot of the Lead’s LinkedIn profile showing Name, Company, and Current Position.”

* Once uploaded, the system should:

  1. **Attach the image to the lead record.**
  2. Automatically **route the image file to the QA & Leads Portal** for **AI-powered image analysis and validation.**
  3. The AI validation module should:

     * Extract and analyze key text regions (Name, Company, Title).
     * Compare extracted data with the CRM Lead Record.
     * Assign a **Confidence Score (0–100%)** for identity and employment validation.
     * Return validation result flags:

       * ✅ *Verified Match*
       * ⚠️ *Partial Match – Review Needed*
       * ❌ *Mismatch / Invalid Proof*

* Upon successful submission:

  * System logs the verification type = **LinkedIn Verified**
  * Stores:

    * Uploaded file reference
    * AI validation report (JSON payload)
    * Agent ID and Timestamp
  * Updates lead record with verification status (e.g., “Pending QA Review” → “AI Verified” → “Approved”)

---

### 🔹 If “On-Call Confirmation” is Selected:

Display a **New Contact Creation Form** with the following fields:

| Field                       | Input Type   | Validation |
| --------------------------- | ------------ | ---------- |
| First Name                  | Text         | Required   |
| Last Name                   | Text         | Required   |
| Job Title                   | Text         | Required   |
| Email                       | Email format | Required   |
| LinkedIn URL                | URL format   | Optional   |
| Current Position Screenshot | File upload  | Required   |

Upon submission:

1. System **creates a new contact record** for the specified company.

2. **Links the call ID and recording** for the disposition.

3. Stores all verification metadata including:

   * Verification Type = **On-Call Confirmed**
   * Agent ID
   * Timestamp
   * Call ID + Recording Reference

4. **(Optional)**: Uploaded screenshot may also be passed to **QA & Leads Portal** for AI-assisted validation, ensuring title and company match where applicable.

---

### 🔹 QA & Leads Portal Integration (AI Validation Flow)

**Input:** Screenshot from Agent Console
**Process:**

* OCR-based text extraction
* AI comparison with CRM Lead Record
* Confidence scoring (Name, Company, Title match %)
* Fraud / mismatch detection (e.g., outdated job, unrelated company, or duplicate contact)

**Output:**
AI Validation JSON returned to CRM:

```json
{
  "lead_id": "L-87213",
  "verification_type": "LinkedIn Verified",
  "validation_confidence": 96.4,
  "status": "AI Verified",
  "comments": "Profile data matches CRM record within 96% confidence."
}
```

**Final Statuses:**

* ✅ Verified and Approved
* ⚠️ Flagged for QA Review
* ❌ Rejected (Invalid Proof or Mismatch)

---

### 🔹 Summary of System Behavior:

1. **Mandatory Verification Step** before any “Qualified” submission.
2. Two verification paths:

   * **LinkedIn Verification** (with AI + QA validation)
   * **On-Call Verification** (manual + optional AI check)
3. Seamless **data flow between Agent Console → CRM → QA & Leads Portal.**
4. **Full audit trail** including:

   * Agent actions
   * Uploaded assets
   * AI analysis
   * Call records and timestamps

---

Would you like me to now:

* ✅ Convert this into a **technical functional spec** (with event triggers, API endpoints, and data flow schema),
  or
* 🧩 Create a **UI workflow wireframe** showing how this verification flow would appear in the Agent Console and QA Portal?

Either option would make it directly actionable for your product and engineering teams.